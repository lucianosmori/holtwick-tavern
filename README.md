# Holtwick Tavern

A small fantasy village rendered in Phaser 3 where you can chat with two NPCs whose dialogue is generated by a local LLM running entirely in your browser. No server, no API keys, no token billing — the model downloads once (~1 GB) and lives in your browser's IndexedDB after that.

**[▶ Live demo](https://lucianosmori.github.io/holtwick-tavern/)** *(WebGPU required — Chrome / Edge / Safari 18+)*

## What's in it

- **Edda the Blacksmith** — terse, in-character forge owner. She answers in 1–2 sentences and won't talk about anything outside the village.
- **Finn the Tavern Keeper** — gossipy, drops village rumors freely, longer replies, refers to ale and the hearth.

Walk up to either NPC, press <kbd>E</kbd> (desktop) or tap the **TALK** button (mobile), and chat. Each NPC keeps its own conversation history within a session. Switching back to the other NPC resumes that thread.

## Stack

| Layer | Choice | Notes |
|---|---|---|
| 2D engine | [Phaser 3.80](https://phaser.io/) | Loaded from CDN. Top-down scene, custom DOM virtual joystick on touch. |
| In-browser LLM | [WebLLM 0.2.79](https://github.com/mlc-ai/web-llm) | OpenAI-compatible streaming API, runs models locally on WebGPU. |
| Model | [Qwen2.5-1.5B-Instruct (q4f16_1)](https://huggingface.co/mlc-ai/Qwen2.5-1.5B-Instruct-q4f16_1-MLC) | ~1 GB quantized weights, served from MLC's HuggingFace mirror. |
| Persona format | JSON in `npcs/<id>.json` | `{ id, name, label, opening, system_prompt, few_shots[], model: { temperature, max_tokens } }`. Adding NPCs is data-only — drop a file in `npcs/`, add an entry to the scene's `this.npcs[]`. |

## Running locally

The page is fully static. Any HTTP server works (`fetch()` won't load `npcs/*.json` from `file://`):

```bash
# Python 3
python -m http.server 8000

# Or Node
npx http-server .
```

Open http://localhost:8000 and walk up to either colored square.

**First load** downloads ~1 GB of model weights. Subsequent visits hit IndexedDB and start instantly.

## Browser support

| Browser | Status |
|---|---|
| Chrome 113+ | ✓ |
| Edge 113+ | ✓ |
| Safari 18+ (macOS / iOS) | ✓ |
| Firefox | ✗ — WebGPU not yet enabled by default. The page renders a graceful "WebGPU unavailable" message and the world stays explorable. |

## Controls

| Action | Desktop | Mobile |
|---|---|---|
| Move | <kbd>WASD</kbd> / arrow keys | Virtual joystick (bottom-left) |
| Talk | <kbd>E</kbd> when near an NPC | **TALK** button (bottom-right) |
| Send chat | <kbd>Enter</kbd> | Send button |
| Close dialog | <kbd>Escape</kbd> / tap outside | Close button |

## Adding a new NPC

1. Create `npcs/<id>.json`:
   ```json
   {
     "id": "ada",
     "name": "Ada",
     "label": "Ada the Herbalist",
     "opening": "First time at my hut? You look tired.",
     "system_prompt": "You are Ada the herbalist of Holtwick. You speak gently…",
     "few_shots": [
       { "user": "What do you sell?", "assistant": "Tinctures, mostly. Salves for the road." }
     ],
     "model": { "temperature": 0.8, "max_tokens": 130 }
   }
   ```
2. In `index.html`, add an entry to `this.npcs = [...]` inside `WorldScene.create`:
   ```js
   { id: 'ada', name: 'Ada', x: 1100, y: 800, color: 0x6ad55c },
   ```

That's all. Persona, opening line, few-shots, and model knobs are data; the dialog flow stays the same.

## License

MIT — see [LICENSE](LICENSE).

The Qwen model is released under [Apache 2.0](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct/blob/main/LICENSE) by Alibaba; WebLLM is [Apache 2.0](https://github.com/mlc-ai/web-llm/blob/main/LICENSE) by the MLC project; Phaser is [MIT](https://github.com/phaserjs/phaser/blob/master/LICENSE.txt).

## Origin

Built as a contained POC inside a private R&D sandbox over a few iterations: a Phaser scene with one NPC → wiring WebLLM with a single hardcoded persona → externalizing the persona to JSON and adding a second NPC with per-NPC chat history. Repackaged as a standalone public demo because the result is small, self-contained, and shows what's possible when the entire dialog stack runs in the browser with no cloud dependency.
